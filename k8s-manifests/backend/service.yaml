apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: onetouch
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80          # Exposed port inside the cluster
      targetPort: 3000  # Container port
  type: ClusterIP
